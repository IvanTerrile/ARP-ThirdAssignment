Scriviamo qui i problemi trovati, così non ce ne dimentichiamo:

2) controllare se i nuovi errori introdotti con i socket vengono scritti nel log file o comunque che si vedano 
   da qualche parte quando vengono stampati.

//////////////////////////////////////////////////////////////////////
Per collegarsi ad una WSL remota usata come SERVER:
aprire una powershell e eseguire questa riga di codice prima di eseguire il resto.
netsh interface portproxy add v4tov4 listenport=4000 listenaddress=0.0.0.0 connectport=4000 connectaddress="indirizzo IPv4 dell PC usato come SERVER"